# 服务器使用指南（实战版）

## 1. 连接

| 项目 | 值 |
|------|-----|
| td3 (2080Ti ×2) | `ssh -p 10022 gis2026@10.130.11.68` |
| td4 (3090 ×2) | `ssh -p 10022 gis2026@10.130.11.69` |
| 密码 | 不写入仓库；请从密码管理器或组内安全渠道读取 |

VS Code Remote-SSH 配置（`~/.ssh/config`）：

```
Host td3
  HostName 10.130.11.68
  Port 10022
  User gis2026

Host td4
  HostName 10.130.11.69
  Port 10022
  User gis2026
```

## 2. 服务器环境

- **td3**: 2080 Ti 11GB ×2, 256GB RAM
- **td4**: 3090 24GB ×2, 256GB RAM
- **OS**: Ubuntu 22.04
- **CUDA**: 12.4
- **无外网**，装包需配置浙大镜像源

## 3. 目录结构

```
~/                     # SSD，空间小 → 只放 .bashrc 等配置
~/local/               # 外挂机械硬盘 → 所有工作放这里
  conda_envs/          # conda 环境目录
    presl/             # 本项目环境
  pressl-cropseg-mirror/  # 项目代码
    data/cache/galileo-base-patch8/  # 特征缓存
    logs/              # TensorBoard 日志
    checkpoints/       # 模型权重
    runlogs/           # 训练 stdout 日志
```

## 4. 环境激活

```bash
conda run -n presl python -B scripts/xxx.py ...
```

环境名 `presl`，Python 3.10，PyTorch 已装好。

## 5. 常用命令速查

### 看 GPU

```bash
nvidia-smi
```

### 看谁在跑什么

```bash
ps aux | grep train_cached | grep -v grep
```

### 杀训练进程

```bash
# 先确认再杀（每个训练有两个进程：conda wrapper + python）
kill <conda_pid> <python_pid>
```

### 启动 TensorBoard

```bash
cd ~/local/pressl-cropseg-mirror

# 前台（调试用）
conda run -n presl tensorboard --logdir logs --bind_all --port 6006

# 后台
nohup conda run -n presl tensorboard --logdir logs --bind_all --port 6006 > /dev/null 2>&1 &
```

浏览器访问 `http://10.130.11.69:6006`（端口被占就换 6007、6008）。

### 确认端口占用

```bash
ss -tlnp | grep 6006
```

## 6. 训练命令

### CA-HPI 注意力基线冒烟（1 epoch 2 batch）

当前研究固定使用 3D-Aware 视觉基线，并通过 `--prior-config` 在 decoder 前接入 CA-HPI：

```bash
cd ~/local/pressl-cropseg-mirror

conda run -n presl python -u -B scripts/train_cached.py \
  --config configs/galileo_3d_aware_dpt.yaml \
  --prior-config configs/prior_injection/ca_hpi_structured.yaml \
  --train-cache-dir data/cache/galileo-base-patch8/monthly12_tile64_patch4_hl3-6-9-12_temporal-v2_tfp16_train \
  --val-cache-dir data/cache/galileo-base-patch8/monthly12_tile64_patch4_hl3-6-9-12_temporal-v2_tfp16_val \
  --cache-format temporal_v2 --temporal-dtype float16 \
  --batch-size 1 \
  --max-train-batches 2 --max-val-batches 2 --epochs 1 \
  --device cuda:0
```

成功时除常规 loss/metrics 外，还会打印 `prior_diagnostics`。运行目录内应生成：

```text
prior_diagnostics_history.json
prior_diagnostics_history.csv
events.out.tfevents.*              # 含 prior/train/... 与 prior/val/...
```

第一次训练批次的 strength 可以为 0；完成第一次 optimizer step 后，验证阶段 strength 应离开 0。`applied_residual_ratio` 才是真正进入 decoder 的先验残差比例。

### CA-HPI M1 注意力基线（最多 50 epoch）

确认 2-batch 冒烟通过后，优先运行这一项，不等待 FiLM 或其他消融：

```bash
cd ~/local/pressl-cropseg-mirror

nohup conda run -n presl python -u -B scripts/train_cached.py \
  --config configs/galileo_3d_aware_dpt.yaml \
  --prior-config configs/prior_injection/ca_hpi_structured.yaml \
  --train-cache-dir data/cache/galileo-base-patch8/monthly12_tile64_patch4_hl3-6-9-12_temporal-v2_tfp16_train \
  --val-cache-dir data/cache/galileo-base-patch8/monthly12_tile64_patch4_hl3-6-9-12_temporal-v2_tfp16_val \
  --cache-format temporal_v2 --temporal-dtype float16 \
  --epochs 50 --device cuda:0 \
  > runlogs/m1_ca_hpi_structured_v1_seed42.log 2>&1 &
```

第一结果固定使用：

```text
checkpoints/galileo_3d_aware_dpt_native_skip_late_fusion_seed42_prior_ca_hpi_structured_v1_cached/best_val_miou.pt
```

训练结束后先回传 `training_history.csv`、`prior_diagnostics_history.csv`、最佳 epoch/fold4 mIoU 和完整命令。checkpoint 已由 fold4 固定后，才运行一次 fold5 test；不根据 test 修改先验 v1 或重选 epoch。

### 当前主方法：SA-SFiLM（来源分层注意力 + 空间—通道 FiLM）

先在空闲 GPU 做 2-batch 冒烟：

```bash
cd ~/local/pressl-cropseg-mirror

conda run --no-capture-output -n presl python -u -B scripts/train_cached.py \
  --config configs/galileo_3d_aware_dpt.yaml \
  --prior-config configs/prior_injection/sa_spatial_film_m1_m2_m3_m4.yaml \
  --train-cache-dir data/cache/galileo-base-patch8/monthly12_tile64_patch4_hl3-6-9-12_temporal-v2_tfp16_train \
  --val-cache-dir data/cache/galileo-base-patch8/monthly12_tile64_patch4_hl3-6-9-12_temporal-v2_tfp16_val \
  --cache-format temporal_v2 --temporal-dtype float16 \
  --batch-size 1 --max-train-batches 2 --max-val-batches 2 \
  --epochs 1 --device cuda:0
```

成功条件：四层 `strength` 从约 `0.01` 开始，`applied_residual_ratio` 从第一轮即非零；
诊断包含 `film_scale_abs_mean`、`film_shift_abs_mean`，以及 M1/M2/M3/M4 各自的
`attention_mass`，四个来源权重之和约为 1。

正式训练：

```bash
cd ~/local/pressl-cropseg-mirror
mkdir -p runlogs

nohup conda run --no-capture-output -n presl python -u -B scripts/train_cached.py \
  --config configs/galileo_3d_aware_dpt.yaml \
  --prior-config configs/prior_injection/sa_spatial_film_m1_m2_m3_m4.yaml \
  --train-cache-dir data/cache/galileo-base-patch8/monthly12_tile64_patch4_hl3-6-9-12_temporal-v2_tfp16_train \
  --val-cache-dir data/cache/galileo-base-patch8/monthly12_tile64_patch4_hl3-6-9-12_temporal-v2_tfp16_val \
  --cache-format temporal_v2 --temporal-dtype float16 \
  --epochs 50 --device cuda:0 \
  > runlogs/sa_spatial_film_m1_m2_m3_m4_seed42.log 2>&1 &

echo $!
```

正式 checkpoint：

```text
checkpoints/galileo_3d_aware_dpt_native_skip_late_fusion_seed42_prior_sa_spatial_film_m1_m2_m3_m4_v1_cached/best_val_miou.pt
```

### 历史 Global Add 命令（LEGACY，仅用于复现）

### P0（无物候先验）

```bash
cd ~/local/pressl-cropseg-mirror && nohup bash -c 'conda run -n presl python -u -B scripts/train_cached.py --config <decoder_config> --train-cache-dir data/cache/galileo-base-patch8/monthly12_tile64_patch4_hl3-6-9-12_temporal-v2_tfp16_train --val-cache-dir data/cache/galileo-base-patch8/monthly12_tile64_patch4_hl3-6-9-12_temporal-v2_tfp16_val --cache-format temporal_v2 --temporal-dtype float16 --device cuda:0' > logs/p0_xxx.log 2>&1 &
```

### P1（正确外部先验）

```bash
cd ~/local/pressl-cropseg-mirror && nohup bash -c 'conda run -n presl python -u -B scripts/train_cached.py --config <decoder_config> --phenology-config configs/phenology/external.yaml --train-cache-dir data/cache/galileo-base-patch8/monthly12_tile64_patch4_hl3-6-9-12_temporal-v2_tfp16_train --val-cache-dir data/cache/galileo-base-patch8/monthly12_tile64_patch4_hl3-6-9-12_temporal-v2_tfp16_val --cache-format temporal_v2 --temporal-dtype float16 --device cuda:0' > logs/p1_xxx.log 2>&1 &
```

### P2（类别置乱先验）

```bash
cd ~/local/pressl-cropseg-mirror && nohup bash -c 'conda run -n presl python -u -B scripts/train_cached.py --config <decoder_config> --phenology-config configs/phenology/external_class_shuffled.yaml --train-cache-dir data/cache/galileo-base-patch8/monthly12_tile64_patch4_hl3-6-9-12_temporal-v2_tfp16_train --val-cache-dir data/cache/galileo-base-patch8/monthly12_tile64_patch4_hl3-6-9-12_temporal-v2_tfp16_val --cache-format temporal_v2 --temporal-dtype float16 --device cuda:0' > logs/p2_xxx.log 2>&1 &
```

### Decoder 选项（替换 `<decoder_config>`）

| 方案 | config |
|------|--------|
| Single-layer DPT + Temporal Readout | `configs/galileo_single_layer_dpt_temporal_readout.yaml` |
| Multi-layer DPT + Temporal Readout | `configs/galileo_multi_layer_dpt_temporal_readout.yaml` |
| UPerNet + Temporal Readout | `configs/galileo_upernet_temporal_readout.yaml` |
| Galileo-Adapted DPT + Temporal Readout | `configs/galileo_adapted_dpt_temporal_readout.yaml` |
| 3D-Aware DPT | `configs/galileo_3d_aware_dpt.yaml` |

### 评估

```bash
conda run -n presl python -u -B scripts/eval_cached.py \
  --config configs/galileo_3d_aware_dpt.yaml \
  --prior-config configs/prior_injection/ca_hpi_structured.yaml \
  --checkpoint <path/to/best_val_miou.pt> \
  --cache-dir data/cache/galileo-base-patch8/monthly12_tile64_patch4_hl3-6-9-12_temporal-v2_tfp16_test \
  --split test \
  --cache-format temporal_v2 --temporal-dtype float16 \
  --device cuda:0 \
  --output-dir output/<实验名>
```

旧 Global Add checkpoint 才使用 `--phenology-config`；它与 `--prior-config` 不得同时传入。正式方案未由 fold4 固定前，不运行 fold5 test。

## 7. 并行训练注意事项

- **双 GPU 可并行**：`--device cuda:0` 和 `--device cuda:1` 各跑一个实验
- **不要同卡跑多个**：OOM 风险，每个单层 DPT 约 6-7GB，多进程 I/O 争抢机械盘也慢
- **启动后验证**：`nvidia-smi | grep python` 确认显存在涨（不是卡在初始化）
- **TensorBoard events 文件 >500 字节** 才说明真正在跑

## 8. 常见故障排查

| 现象 | 原因 | 解决 |
|------|------|------|
| TensorBoard 连不上 | 端口被占 | 换 `--port 6007`，用 `ss -tlnp` 查占用 |
| events 文件 88 字节 | 训练死在第一个 epoch 前 | OOM 或 DataLoader 崩，换空闲 GPU 重来 |
| 日志只有 "nohup: ignoring input" | bash -c 吞 stdout | 正常，看 TensorBoard 就行 |
| 显存不动（<1GB 持续很久） | 数据加载慢 | 机械盘 I/O 瓶颈，等或减少并发进程 |
| 多个旧进程堆积 | 前次 nohup 没杀干净 | `ps aux \| grep train_cached`，kill 对应 PID |
| conda 找不到包 | 无外网 | 配浙大源或用 `pip install -i https://mirrors.zju.edu.cn/pypi/simple` |

## 9. 协作公约

1. **动前先看 GPU 占用**：`nvidia-smi` 确认不抢别人正在跑的卡
2. **跑完或停实验及时 kill 进程**：别占着显存不做事
3. **TensorBoard 统一用 `--logdir logs`**：所有人共享同一个 logs 目录，选择不同端口
4. **不要往 `~/` 放大文件**：SSD 空间有限
5. **模型和数据放 `~/local/`**：外挂硬盘，空间大
6. **命名要可追溯**：nohup 日志 > `logs/`，checkpoint 自动按 config+phenology 分目录
